# ND Explorer 저장 공간 분석 기능 계획

## 목표

- 감지된 모든 드라이브를 엑셀 시트 형태의 하단 탭으로 전환한다.
- 폴더와 파일 크기를 트리맵 면적으로 시각화한다.
- 분석 중 파일이 삭제되거나 이동되어도 분석과 프로그램 실행을 계속한다.
- 선택한 위치의 변경을 감지해 분석 결과를 자동으로 갱신한다.
- 실행 시 UAC를 통해 관리자 권한을 요청한다.

## 구조

1. `StorageScanService`
   - UI 스레드와 분리된 취소 가능한 비동기 스캔
   - 재분석 지연 병합과 진행 상황 보고
   - 접근 거부, 경로 소멸, 이동, 장치 제거, 긴 경로 예외를 항목 단위로 건너뜀
   - 재분석 순환을 막기 위해 재분석 요청을 하나로 병합
2. `DriveAnalysisViewModel`
   - 드라이브별 현재 경로, 결과, 진행 상태를 독립 보관
   - 선택된 드라이브에만 `FileSystemWatcher`를 연결
   - 변경 알림 폭주와 버퍼 오류를 팝업 없이 지연 재분석으로 복구
3. `StorageTreemapControl`
   - 항목 크기 비율에 따라 이진 분할 트리맵 렌더링
   - 폴더/파일 색상 구분, 선택·호버·툴팁 제공
   - 폴더 더블클릭 탐색
4. `StorageAnalyzerWindow`
   - 드라이브 하단 탭, 상위 폴더, 새로고침, 취소, 상태 표시
   - 드라이브 연결과 제거를 주기적으로 다시 감지
5. `app.manifest`
   - `requireAdministrator`로 앱 시작 시 관리자 권한 요청

## 안전 원칙

- 분석 기능은 읽기 전용이며 파일을 변경하지 않는다.
- 파일 시스템 변경은 정상 상황으로 취급하고 오류 대화상자를 반복 표시하지 않는다.
- 재분석과 창 종료 시 기존 작업을 취소하고 감시자를 해제한다.
- 재분석 도중에도 마지막 정상 결과는 유지한다.
