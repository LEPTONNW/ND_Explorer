<Window x:Class="ND_Explorer.Views.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="입력"
        Width="440"
        SizeToContent="Height"
        MinHeight="170"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,14" />
        <TextBox x:Name="ValueBox" Grid.Row="1" MinHeight="34" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="취소" Width="78" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="확인" Width="78" IsDefault="True" Click="Accept_OnClick"
                    Background="{DynamicResource AccentBrush}" Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
