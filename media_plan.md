# ND Explorer 미디어 기능 구현 계획

## 목표

탐색기에서 이미지와 동영상을 외부 프로그램 없이 확인하고, 배포물 안에서 사용 중인 오픈소스 구성요소와 라이선스를 명확히 확인할 수 있게 한다.

## 구현 범위

1. 미디어 파일 분류
   - 이미지와 동영상 확장자를 모델 수준에서 분류한다.
   - 일반 파일은 기존 Windows 연결 프로그램으로 연다.

2. 이미지 기능
   - 파일 선택 시 오른쪽 패널에서 메모리 사용량을 제한한 썸네일을 비동기로 표시한다.
   - 이미지 더블클릭 시 내부 이미지 뷰어를 연다.
   - 이미지 뷰어는 확대·축소·회전·화면 맞춤·전체화면·최소화를 제공한다.

3. 동영상 기능
   - `LibVLCSharp.WPF 3.10.1`과 `VideoLAN.LibVLC.Windows 3.0.23.1`을 사용한다.
   - LGPL 패키지만 사용하고 `.GPL` 패키지는 참조하지 않는다.
   - 재생/일시정지, 정지, 반복, 10초 탐색, 볼륨, 전체화면, 최소화를 제공한다.

4. 오픈소스 고지
   - 메인 화면에서 별도 고지 창을 열 수 있게 한다.
   - 저작자, 구성요소, 정확한 버전, 라이선스, 소스 위치와 주요 LGPL 조건을 표시한다.

5. 검증
   - 패키지 복원, Debug/Release 빌드, 포맷 검사, 프로그램 기동 검사를 수행한다.
   - 이미지 로딩과 미디어 창 생성 실패가 탐색기 전체 종료로 이어지지 않게 한다.
