# ND Explorer 프로젝트 마스터 문서

> 최종 갱신: 2026-09-06  
> 기준 상태: 파일 탐색기 MVP, 내장 이미지·동영상 뷰어, 저장 공간 분석 기능 구현 및 Release 빌드 완료

---

## 1. 프로젝트 개요

ND Explorer는 Windows 환경에서 파일 탐색, 미디어 확인, 파일 작업, 저장 공간 분석을 한 프로그램에서 수행하기 위한 데스크톱 파일 관리자다.

현재 제품의 핵심 방향은 다음과 같다.

- 어두운 테마와 높은 정보 밀도를 제공한다.
- 파일 시스템 작업을 비동기로 처리해 UI 멈춤을 최소화한다.
- 이미지와 동영상을 외부 프로그램 없이 바로 확인한다.
- 드라이브 용량을 트리맵으로 시각화한다.
- 파일이 분석 중 삭제되거나 이동되어도 프로그램이 종료되지 않게 한다.
- 마우스와 키보드 조작을 함께 제공한다.
- 오픈소스 라이브러리의 버전, 저작자, 라이선스, 소스 위치를 프로그램에서 확인할 수 있게 한다.

제품 기준 문장은 다음과 같다.

> 파일을 찾고, 확인하고, 관리하고, 공간 사용량까지 분석하는 통합 Windows 파일 도구를 만든다.

---

## 2. 기술 구성

| 항목 | 현재 값 |
|---|---|
| 언어 | C# |
| UI 프레임워크 | WPF |
| 대상 프레임워크 | `net9.0-windows` |
| 출력 형식 | Windows GUI 실행 파일 (`WinExe`) |
| 대상 아키텍처 | x64 |
| MVVM | 자체 최소 구현 |
| 동영상 엔진 | LibVLCSharp.WPF + VideoLAN LibVLC |
| 관리자 권한 | 앱 시작 시 `requireAdministrator` 요청 |
| 기본 테마 | 다크 테마 |

NuGet 패키지는 다음 버전으로 고정되어 있다.

| 패키지 | 버전 | 라이선스 |
|---|---:|---|
| `LibVLCSharp.WPF` | `3.10.1` | GNU LGPL 2.1 or later |
| `VideoLAN.LibVLC.Windows` | `3.0.23.1` | GNU LGPL 2.1 or later |

GPL 플러그인을 추가로 포함하는 `VideoLAN.LibVLC.Windows.GPL` 패키지는 사용하지 않는다.

---

## 3. 현재 구현 상태 요약

### 완료된 기능

- 단일 패널 파일 탐색기
- 비동기 파일 및 폴더 열거
- 뒤로, 앞으로, 상위 폴더, 새로고침
- 주소창 직접 이동
- 즐겨찾기, 드라이브, 최근 경로
- 이름 필터, 정렬, 숨김 항목 표시
- 새 폴더, 이름 변경, 복사, 잘라내기, 붙여넣기
- 휴지통 삭제 및 영구 삭제
- 다중 선택과 기본 드래그 앤 드롭
- 창 크기, 위치, 마지막 경로 등 설정 저장
- 우측 이미지 미리보기
- 내장 이미지 뷰어
- VLC 기반 내장 동영상 플레이어
- 라이브러리 라이선스 고지 창
- 드라이브별 저장 공간 분석 및 트리맵
- 파일 시스템 변경 감지와 지연 재분석
- UAC 관리자 권한 요청
- x64 Release 빌드

### 아직 구현하지 않은 주요 확장 기능

- 일반 파일 탐색기의 다중 탭
- 듀얼 패널 탐색
- 작업공간 저장 및 복원
- 명령 팔레트
- 파일 내용 검색 및 인덱싱
- 중복 파일 탐색
- 압축 파일 내부 탐색
- Git 상태 표시
- 플러그인 구조
- 설치 프로그램과 자동 업데이트

---

## 4. 메인 화면

현재 메인 화면은 다음 영역으로 구성된다.

```text
┌ ND EXP ─ 현재 경로 ───────────── 저장 공간 분석 │ 라이브러리 라이센스 고지 ┐
├ 뒤로 │ 앞으로 │ 위로 │ 새로고침 │ 주소창                         │ 이동 ┤
├─────────────┬──────────────────────────────┬────────────────────┤
│ 즐겨찾기     │ 파일 작업 도구               │ 이미지 미리보기       │
│ 드라이브     ├──────────────────────────────┤ 선택 항목 정보         │
│ 최근 위치    │ 이름 필터 / 정렬 / 숨김 항목  │ 내부 뷰어 실행 버튼    │
│              ├──────────────────────────────┤                    │
│              │ 이름 / 수정일 / 유형 / 크기   │                    │
├─────────────┴──────────────────────────────┴────────────────────┤
│ 항목 수 │ 선택 수 │ 상태 및 파일 작업 진행                         │
└────────────────────────────────────────────────────────────────┘
```

### 상단 브랜딩과 기능 버튼

- 기존 `ND` 사각 로고를 `ND EXP` 워드마크로 교체했다.
- 어두운 그라데이션 바탕에 그림자와 하이라이트 텍스트를 겹쳐 음각 효과를 냈다.
- `저장 공간 분석` 버튼으로 분석 창을 연다.
- `라이브러리 라이센스 고지` 버튼으로 오픈소스 고지 창을 연다.
- 저장 공간 분석 창은 중복 생성하지 않고 이미 열려 있으면 기존 창을 활성화한다.

### 파일 목록 헤더

- `이름`, `수정한 날짜`, `유형`, `크기` 열을 클릭해 정렬할 수 있다.
- 헤더 위에 마우스를 올리면 배경과 글자가 밝아진다.
- 클릭 중에는 파란 강조색으로 상태를 표시한다.
- 클릭 가능한 영역에는 손 모양 커서를 표시한다.

---

## 5. 파일 탐색 기능

### 탐색

- 주소창에 절대 경로를 입력하고 이동할 수 있다.
- 뒤로, 앞으로, 상위 폴더 이동을 제공한다.
- 최근 방문 경로를 기록한다.
- 드라이브와 즐겨찾기를 왼쪽 사이드바에 표시한다.
- 접근할 수 없거나 사라진 경로는 오류 상태로 처리하며 앱 전체를 종료하지 않는다.
- 명령줄 첫 번째 파일 인자로 실행하면 해당 이미지나 동영상을 내부 뷰어로 연다.

### 목록 표시

- 이름, 수정한 날짜, 유형, 크기를 표시한다.
- 이름, 수정일, 유형, 크기를 기준으로 정렬할 수 있다.
- 오름차순과 내림차순을 선택할 수 있다.
- 현재 폴더에서 이름을 즉시 필터링한다.
- 숨김 항목 표시를 전환할 수 있다.
- 행 및 열 가상화와 컨테이너 재사용을 활성화했다.
- 대량 항목 반영에는 `BulkObservableCollection`을 사용한다.

### 파일 작업

- 파일 실행 및 폴더 열기
- 새 폴더 생성
- 이름 변경
- 복사
- 잘라내기
- 붙여넣기
- 기본 삭제 시 휴지통 사용
- 확인 후 영구 삭제
- 여러 항목 선택
- 파일 드래그 앤 드롭
- 작업 취소
- 파일·폴더 우클릭 메뉴는 다크 테마 전용 레이아웃으로 표시하며, 메뉴명과 단축키 표기를 분리 정렬하고 마우스 오버·비활성 상태를 시각적으로 구분한다.

파일 작업 실패는 가능한 한 항목 또는 작업 단위로 표시하며, 하나의 오류가 앱 전체 종료로 이어지지 않도록 한다.

### 메인 탐색기 단축키

| 단축키 | 동작 |
|---|---|
| `Ctrl+L` | 주소창 포커스 및 전체 선택 |
| `Ctrl+F` | 이름 필터 포커스 및 전체 선택 |
| `Alt+Left` | 뒤로 이동 |
| `Alt+Right` | 앞으로 이동 |
| `Backspace` | 뒤로 이동(주소창·검색창 입력 중에는 문자 삭제 우선) |
| `Alt+Up` | 상위 폴더 이동 |
| `F5` | 새로고침 |
| `Ctrl+Shift+N` | 새 폴더 |
| 문자 입력 | 입력한 문자 또는 짧은 접두어로 시작하는 항목에 포커스, 같은 문자 반복 시 다음 일치 항목으로 순환 |
| `Ctrl+C` | 복사 |
| `Ctrl+X` | 잘라내기 |
| `Ctrl+V` | 붙여넣기 |
| `F2` | 이름 변경 |
| `Delete` | 휴지통으로 삭제 |
| `Shift+Delete` | 영구 삭제 |
| `Escape` | 주소 입력 또는 현재 작업 취소 |
| `Enter` | 선택한 파일 또는 폴더 열기 |

---

## 6. 이미지 미리보기 및 이미지 뷰어

### 우측 미리보기

- 파일 목록에서 이미지를 선택하면 우측 패널에 미리보기를 표시한다.
- 미리보기는 비동기로 로드한다.
- 이전 선택의 로딩은 취소한다.
- 디코딩 너비를 제한해 큰 이미지를 그대로 메모리에 올리는 부담을 줄인다.
- 파일 공유 모드를 사용해 다른 프로세스가 파일을 수정하거나 삭제하는 동작을 불필요하게 막지 않는다.
- 선택 항목의 이름, 유형, 크기와 내부 뷰어 실행 버튼을 표시한다.

### 내장 이미지 뷰어

지원 확장자:

`jpg`, `jpeg`, `png`, `bmp`, `gif`, `tif`, `tiff`, `ico`

구현 기능:

- 이미지 더블클릭으로 내부 뷰어 실행
- 확대 및 축소
- 화면 맞춤
- 왼쪽 및 오른쪽 90도 회전
- 전체화면
- 최소화
- 확대된 이미지의 가로·세로 스크롤
- 마우스 휠 확대·축소
- 마우스 포인터 위치를 기준으로 한 확대·축소 위치 보정
- 확대 상태에서 마우스 왼쪽 버튼 드래그로 이미지 이동
- 화면 맞춤 상태에서 창 크기 변경 시 배율 자동 재계산
- 전체화면 진입과 해제 후에도 화면 맞춤 상태 유지
- 수동 확대·축소 시 화면 맞춤 모드 해제 및 수동 배율 유지
- 표시 배율, 이미지 해상도, 파일 크기 표시
- 이미지 로드 후 파일 스트림을 닫아 원본 파일을 잠그지 않음

이미지 뷰어 단축키:

| 입력 | 동작 |
|---|---|
| 마우스 휠 | 포인터 기준 확대·축소 |
| 마우스 왼쪽 드래그 | 확대된 이미지 이동 |
| `+` / `-` | 확대·축소 |
| `Ctrl+0` | 화면 맞춤 |
| `Enter` | 전체화면 진입·해제 |
| `F11` | 전체화면 진입·해제 |
| `Escape` | 전체화면 해제 |
| 이미지 더블클릭 | 전체화면 진입·해제 |

---

## 7. VLC 기반 동영상 플레이어

지원 대상으로 등록된 확장자:

`mp4`, `mkv`, `avi`, `mov`, `webm`, `wmv`, `m4v`, `mpg`, `mpeg`, `ts`, `mts`, `m2ts`, `flv`, `3gp`, `3g2`, `ogv`, `vob`, `asf`, `f4v`

실제 재생 가능 여부는 포함된 LibVLC 코덱과 해당 미디어 상태에 따라 결정된다.

### 재생 기능

- 파일 더블클릭으로 내부 동영상 플레이어 실행
- 재생 및 일시정지
- 정지
- 반복 재생
- 10초, 30초, 1분 단위 탐색
- 진행 위치 슬라이더
- 현재 시간과 전체 재생 시간 표시
- 음량 슬라이더 및 현재 음량 표시
- 전체화면
- `Escape` 최소화
- 영상 화면 더블클릭으로 전체화면 전환
- 일반 창 상태에서 영상 화면을 드래그해 플레이어 창 이동
- 재생 종료 시 반복 상태이면 처음부터 다시 재생
- 창 종료 시 타이머, 이벤트, 미디어, 플레이어, LibVLC 자원 해제

### 동영상 플레이어 단축키

| 단축키 | 동작 |
|---|---|
| `Space` | 재생·일시정지 |
| `Left` / `Right` | 10초 뒤로·앞으로 |
| `Ctrl+Left` / `Ctrl+Right` | 30초 뒤로·앞으로 |
| `Shift+Left` / `Shift+Right` | 1분 뒤로·앞으로 |
| `Up` / `Down` | 음량 5단계 증가·감소 |
| `Enter` | 전체화면 진입·해제 |
| `F11` | 전체화면 진입·해제 |
| `Escape` | 플레이어 최소화 |

### 플레이어 디자인

- 어두운 전용 컨트롤 패널
- 파란색 재생 진행 트랙
- 둥근 슬라이더 핸들
- 슬라이더 호버 및 드래그 강조
- 재생 버튼의 주 버튼 강조
- 이동, 정지, 반복 버튼의 통일된 크기와 간격
- 반복 활성 상태의 파란색 표시
- 음량 조절 전용 패널
- 현재 시간과 전체 시간에 고정폭 글꼴 사용
- 하단 주요 단축키 안내
- 최소 창 크기에서도 버튼과 음량 영역이 겹치지 않도록 너비 조정

---

## 8. 라이브러리 라이선스 고지

메인 창 상단의 `라이브러리 라이센스 고지` 버튼으로 고지 창을 연다.

고지 창에는 다음 내용이 포함된다.

- 라이브러리 이름
- 사용 버전
- 저작자 및 프로젝트 기여자
- 적용 라이선스
- NuGet 패키지 링크
- 정확한 소스 코드 링크
- LGPL 준수 사항 요약
- LGPL 2.1 전문 열기

배포 파일에는 다음 문서를 함께 복사한다.

- `Licenses/THIRD-PARTY-NOTICES.txt`
- `Licenses/LGPL-2.1.txt`

LGPL 대응 방식:

- LibVLCSharp와 LibVLC를 수정하지 않은 동적 라이브러리로 분리 배포한다.
- 네이티브 DLL을 실행 파일과 분리된 `libvlc/win-x64` 폴더에 둔다.
- 사용자가 인터페이스 호환 수정 DLL로 교체할 수 있는 구조를 유지한다.
- 저작권, 라이선스, 버전, 저작자, 소스 위치를 고지한다.
- LGPL 2.1 원문을 배포본에 포함한다.
- LGPL 구성요소를 수정하고 디버깅하기 위한 역공학을 배포 약관에서 금지하지 않아야 한다.
- GPL 플러그인 패키지는 포함하지 않는다.

현재 LGPL 2.1 원문 파일 SHA-256:

`20E50FE7AAE3E56378EBF0417D9DE904F55A0E61E4DF315333E632A4D3555D95`

이 문서의 라이선스 설명은 구현 및 배포 점검용이며 법률 자문을 대신하지 않는다.

---

## 9. 저장 공간 분석

메인 창의 `저장 공간 분석` 버튼으로 별도 분석 창을 연다.

### 드라이브 전환

- `DriveInfo.GetDrives()`를 통해 현재 감지되는 드라이브를 수집한다.
- 각 드라이브를 분석 창 하단 탭으로 표시한다.
- 탭 위치와 동작은 엑셀 시트 전환 방식과 유사하게 구성했다.
- 선택한 드라이브만 활성 분석 및 파일 변경 감시를 수행한다.
- 드라이브 연결과 제거를 4초마다 다시 감지한다.
- 드라이브 탭별로 현재 탐색 경로와 마지막 정상 결과를 독립적으로 유지한다.
- 드라이브 전체 크기, 사용 공간, 여유 공간을 표시한다.

### 트리맵

- 항목 크기가 클수록 더 큰 사각형으로 표시한다.
- 폴더와 파일에 서로 다른 색상 팔레트를 사용한다.
- 면적 비율에 따른 이진 분할 방식으로 사각형을 배치한다.
- 사각형 호버, 선택 테두리, 이름, 크기, 전체 경로 툴팁을 제공한다.
- 폴더 사각형을 한 번 클릭하면 기존 화면을 유지한 채 그 사각형 내부에 직계 파일과 폴더를 펼친다.
- 펼쳐진 하위 폴더를 다시 클릭하면 그 사각형 안에 다음 단계의 파일과 폴더를 중첩 표시한다.
- 폴더별 하위 목록은 클릭 시점에 지연 분석하므로 처음부터 전체 디렉터리 트리를 메모리에 보관하지 않는다.
- 펼친 폴더는 다시 클릭해 접을 수 있으며 계산된 하위 결과는 유지한다.
- 분석 중인 폴더를 다시 클릭하면 해당 하위 분석을 취소한다.
- `▸`와 `▾` 기호로 접힘과 펼침 상태를 구분한다.
- 하위 분석 중에는 폴더 헤더에 `분석 중…` 상태를 표시한다.
- 여러 단계의 하위 사각형을 역순으로 히트 테스트해 가장 안쪽에 있는 항목을 정확히 선택한다.
- 파일이나 폴더를 우클릭하면 `ND Explorer에서 파일/폴더 위치 열기`와 `Windows 탐색기에서 파일/폴더 위치 열기` 메뉴를 표시한다.
- ND Explorer 열기는 폴더라면 해당 폴더로 이동하고, 파일이라면 상위 폴더로 이동한 뒤 파일을 선택하고 화면에 보이게 스크롤한다.
- Windows 탐색기 열기는 파일에 `/select`를 사용하고 폴더는 해당 폴더를 직접 연다.
- 우클릭 메뉴는 기본 Windows 아이콘 여백을 사용하지 않는 전용 다크 템플릿으로 렌더링한다.
- 우클릭 이후 항목이 사라진 경우 앱을 종료하거나 반복 팝업을 띄우지 않고 분석 상태 문구로 안내한다.
- 상위 폴더 이동, 새로고침, 분석 취소를 지원한다.
- 접근할 수 없는 하위 항목이 포함된 폴더에는 `!` 경고를 표시한다.
- 지나치게 많은 항목은 큰 항목 1,199개와 나머지를 합친 `기타 항목` 사각형으로 렌더링해 UI 과부하를 방지한다.

### 스캔 구조

- `StorageScanService`는 UI 스레드 밖에서 동작한다.
- 스캔은 `CancellationToken`으로 취소할 수 있다.
- 폴더 크기는 반복형 스택 탐색으로 계산해 깊은 재귀 호출을 피한다.
- 재분석 지점과 심볼릭 링크는 따라가지 않아 순환 탐색을 방지한다.
- 파일 수, 폴더 수, 누적 크기, 건너뛴 항목 수를 진행 상태로 보고한다.
- 현재 경로의 직접 파일과 폴더를 먼저 발견해 크기 계산 전이라도 트리맵에 표시한다.
- 각 폴더를 분석하는 동안 누적된 중간 크기를 약 150ms 간격으로 트리맵에 반영한다.
- 같은 경로에서 빠르게 발생하는 중간 결과는 최신 값 하나로 병합해 UI 메시지 폭주를 방지한다.
- 진행 중인 항목은 `BulkObservableCollection.UpsertRange`로 배치 갱신한다.
- 전체 스캔이 끝나면 크기 내림차순의 최종 결과로 컬렉션을 한 번 정리한다.
- 취소되었거나 이전 경로에서 늦게 도착한 진행 결과는 스캔 세대 번호와 취소 토큰으로 무시한다.

### 파일 변경과 오류 안전성

분석 중 다음 상황은 정상적인 파일 시스템 경쟁 상태로 취급한다.

- 파일 삭제
- 파일 이동
- 파일 또는 폴더 이름 변경
- 파일 크기 변경
- 접근 권한 변경
- 드라이브 제거
- 긴 경로
- 재분석 지점
- `FileSystemWatcher` 버퍼 오류

처리 원칙:

- 예상 가능한 파일 시스템 예외를 항목 단위로 잡고 다음 항목을 계속 분석한다.
- 반복 오류 팝업을 표시하지 않는다.
- 건너뛴 항목 수를 상태 표시줄에 기록한다.
- 분석 중에는 감시자를 잠시 해제해 변경 이벤트 폭주와 무한 재분석을 줄인다.
- 분석 완료 후 선택 경로에 감시자를 다시 연결한다.
- 변경 이벤트는 2초 동안 병합한 후 한 번만 재분석한다.
- 같은 경로를 자동 또는 수동으로 재분석할 때는 기존 계층 컬렉션을 먼저 비우지 않는다.
- 진행 크기와 최종 정렬 결과를 반영할 때 기존 `Children`, 펼침, 로딩 상태를 경로 기준으로 병합한다.
- 분석 중 면적이 작아진 폴더도 최소 공간이 있으면 하위 사각형 렌더링을 계속한다.
- 변경 중 현재 폴더가 사라지면 존재하는 상위 폴더 또는 드라이브 루트로 복구한다.
- 창을 닫거나 탭을 바꾸면 기존 스캔과 감시자를 취소·해제한다.

저장 공간 분석은 읽기 전용이며 파일을 삭제하거나 이동하지 않는다.

---

## 10. 관리자 권한과 UAC

`app.manifest`에 다음 실행 수준을 지정했다.

```xml
<requestedExecutionLevel level="requireAdministrator" uiAccess="false" />
```

따라서 `ND_Explorer.exe`를 시작하면 Windows UAC가 관리자 권한 승인을 요청한다.

함께 설정된 항목:

- Windows 10 이상 호환성 선언
- Per-monitor DPI 인식
- 긴 경로 인식

주의 사항:

- 사용자가 UAC 승인을 거절하면 앱은 시작되지 않는다.
- 관리자 권한이어도 운영체제가 보호하는 모든 경로를 반드시 읽을 수 있는 것은 아니므로 예외 처리는 계속 필요하다.
- Windows 무결성 수준 정책 때문에 일반 권한 Windows 탐색기에서 관리자 권한 ND Explorer로 끌어 놓기가 차단될 수 있다.
- IDE에서 디버깅할 때도 실행 권한 상승이 필요할 수 있다.

---

## 11. 실제 코드 구조

```text
ND_Explorer/
├─ App.xaml
├─ App.xaml.cs
├─ MainWindow.xaml
├─ MainWindow.xaml.cs
├─ app.manifest
├─ ND_Explorer.csproj
├─ Controls/
│  └─ StorageTreemapControl.cs
├─ Infrastructure/
│  ├─ AsyncRelayCommand.cs
│  ├─ BulkObservableCollection.cs
│  ├─ ObservableObject.cs
│  └─ RelayCommand.cs
├─ Models/
│  ├─ ExplorerSettings.cs
│  ├─ FileSystemItem.cs
│  ├─ MediaFileSupport.cs
│  ├─ NavigationLocation.cs
│  ├─ SortOption.cs
│  └─ StorageEntry.cs
├─ Services/
│  ├─ DialogService.cs
│  ├─ FileOperationService.cs
│  ├─ FileSystemService.cs
│  ├─ FileViewerService.cs
│  ├─ ImagePreviewService.cs
│  ├─ SettingsService.cs
│  ├─ StorageScanService.cs
│  └─ 각 서비스 인터페이스
├─ Themes/
│  ├─ Colors.xaml
│  └─ Controls.xaml
├─ ViewModels/
│  ├─ DriveAnalysisViewModel.cs
│  ├─ ExplorerPaneViewModel.cs
│  ├─ MainWindowViewModel.cs
│  └─ StorageAnalyzerViewModel.cs
├─ Views/
│  ├─ ImageViewerWindow.xaml
│  ├─ OpenSourceNoticesWindow.xaml
│  ├─ StorageAnalyzerWindow.xaml
│  ├─ TextPromptDialog.xaml
│  └─ VideoPlayerWindow.xaml
└─ Licenses/
   ├─ LGPL-2.1.txt
   └─ THIRD-PARTY-NOTICES.txt
```

### 주요 책임

| 구성요소 | 책임 |
|---|---|
| `ExplorerPaneViewModel` | 현재 경로, 목록, 탐색 기록, 선택, 정렬, 필터, 명령 상태 |
| `FileSystemService` | 비동기 디렉터리 열거 및 항목 메타데이터 |
| `FileOperationService` | 복사, 이동, 이름 변경, 삭제, 새 폴더 |
| `FileViewerService` | 파일 형식에 따른 이미지·동영상·외부 실행 분기 |
| `ImagePreviewService` | 취소 가능한 이미지 미리보기 로딩 |
| `VideoPlayerWindow` | LibVLC 수명과 재생 UI 관리 |
| `StorageScanService` | 안전하고 취소 가능한 파일·폴더 크기 계산 |
| `DriveAnalysisViewModel` | 드라이브별 탐색, 스캔, 진행 상태, 변경 감시 |
| `StorageAnalyzerViewModel` | 드라이브 감지, 추가·제거, 선택 탭 관리 |
| `StorageTreemapControl` | 크기 비율 트리맵 렌더링과 항목 히트 테스트 |
| `SettingsService` | 창 상태, 최근 위치 등 사용자 설정 저장 |

---

## 12. 테마 및 디자인 기준

### 공통 색상

| 용도 | 색상 |
|---|---|
| 기본 배경 | `#111318` |
| 패널 배경 | `#181B21` |
| 떠 있는 요소 | `#20242C` |
| 호버 | `#292E38` |
| 선택 상태 | `#2B5EA8` |
| 강조색 | `#4C8DFF` |
| 기본 텍스트 | `#E7EAF0` |
| 보조 텍스트 | `#9DA5B4` |
| 경계선 | `#2A2F39` |
| 오류 | `#FF6B6B` |

### 원칙

- 의미 기반 리소스 이름으로 색상을 관리한다.
- 호버, 누름, 선택, 키보드 포커스를 구분한다.
- 상태를 색상만으로 표현하지 않고 텍스트와 아이콘을 함께 사용한다.
- 파일 목록에는 조밀한 행 높이를 적용한다.
- 긴 이름과 경로는 말줄임과 툴팁으로 처리한다.
- 미디어 플레이어와 트리맵은 기본 테마와 어울리는 전용 다크 스타일을 사용한다.
- 장시간 파일 작업과 분석은 UI 스레드에서 수행하지 않는다.

---

## 13. 안정성 및 성능 원칙

- 폴더 이동 시 이전 열거 작업을 취소한다.
- 이미지 선택이 바뀌면 이전 미리보기 작업을 취소한다.
- 동영상 창 종료 시 네이티브 VLC 자원을 명시적으로 해제한다.
- 파일 목록과 분석 결과는 배치 단위로 반영한다.
- 파일 목록 가상화를 활성화한다.
- 파일 시스템 변경은 오류가 아닌 정상 경쟁 상태로 취급한다.
- 네트워크, 이동식 드라이브, 접근 제한 경로가 앱 전체를 중단시키지 않게 한다.
- 재분석 지점을 따라가지 않는다.
- 변경 감지 이벤트는 지연 병합한다.
- 장시간 분석에는 진행 상태와 취소 기능을 제공한다.
- 창 또는 탭 종료 시 `CancellationTokenSource`, 타이머, 감시자, 이벤트 구독을 정리한다.

---

## 14. 검증 기록

### 빌드

- 구성: `Release`, x64
- 결과: 경고 0개, 오류 0개
- 포맷 검사: `dotnet format --verify-no-changes` 통과
- 최종 실행 파일: `ND_Explorer/bin/Release/net9.0-windows/ND_Explorer.exe`
- VLC 포함 산출물 크기: 약 101.5MB
- 실제 VLC 네이티브 파일은 `win-x64`만 포함
- `win-x86`, `win-arm64` 디렉터리는 패키지 구조상 비어 있을 수 있음

### 미디어

- Windows 샘플 JPG로 이미지 뷰어 프로세스 실행 확인
- 실제 MP4로 동영상 플레이어 실행 확인
- 실행 프로세스에서 `libvlc.dll`과 `libvlccore.dll` 로드 확인
- 새 동영상 플레이어 XAML 템플릿 로딩 확인
- 라이선스 원문과 제3자 고지문이 Release 출력에 복사되는 것 확인

### 저장 공간 분석

- 분석 중 파일 생성·이동·삭제를 반복하는 동시 변경 테스트 수행
- 테스트 데이터: 폴더 120개, 파일 7,200개
- 결과: 분석 정상 완료
- 경합으로 사라진 항목 2건은 예외 팝업이나 프로세스 종료 없이 건너뜀
- 점진 표시 테스트 데이터: 폴더 80개, 파일 8,000개
- 전체 완료 전에 진행 결과 84개 배치와 항목 갱신 160건이 전달되는 것을 확인
- 가상 3단계 폴더 구조로 상위 펼침, 하위 펼침, 하위 접기와 계산 결과 유지 확인
- 자동 재분석의 중간 크기 갱신과 최종 목록 교체 후에도 펼친 하위 트리가 유지되는 회귀 테스트 통과
- 숨김 개발 실행과 UI 자동화를 통해 분석 창 및 드라이브 탭 생성 확인
- Release 실행 파일에서 `requireAdministrator` 매니페스트 문자열 확인

### 관리자 권한 실행 검증 시 주의

검증 당시 자동화 셸은 일반 권한이었다. 최종 EXE 직접 실행은 사용자 화면에 UAC를 발생시키므로 자동 실행하지 않았고, 매니페스트 포함 여부를 실행 파일에서 확인했다. 실제 사용자 실행 시 UAC 승인 후 분석 창의 장시간 실디스크 동작을 추가 확인해야 한다.

---

## 15. 빌드 및 실행

프로젝트 루트:

```text
C:\Codex\repo\ND_Explorer
```

Release 빌드:

```powershell
dotnet build .\ND_Explorer\ND_Explorer.csproj --configuration Release
```

포맷 검사:

```powershell
dotnet format .\ND_Explorer\ND_Explorer.csproj --verify-no-changes --no-restore
```

실행 파일:

```text
C:\Codex\repo\ND_Explorer\ND_Explorer\bin\Release\net9.0-windows\ND_Explorer.exe
```

실행하면 UAC 관리자 권한 확인 창이 나타난다.

---

## 16. 다음 권장 작업

우선순위 순서:

1. 실제 대용량 시스템 드라이브에서 저장 공간 분석 시간과 메모리 사용량 측정
2. 분석 중 지속적으로 변경되는 시스템 폴더의 자동 재분석 빈도 조정
3. 트리맵에서 파일 형식별 범례와 확장자 필터 추가
4. 트리맵 우클릭 메뉴와 현재 파일 탐색기 위치로 이동 기능 추가
5. 저장 공간 분석 결과 캐시와 이전 스냅샷 비교
6. 일반 탐색기 다중 탭 구현
7. 듀얼 패널과 작업공간 구현
8. 파일 작업 충돌 처리 UI 개선
9. 설치 패키지, 코드 서명, 업데이트 방식 결정
10. UAC 상시 실행으로 제한되는 외부 드래그 앤 드롭의 대체 가져오기 UI 설계

---

## 17. 배포 전 체크리스트

- [ ] 실제 사용자 계정에서 UAC 승인 및 거절 동작 확인
- [ ] 1TB 이상 드라이브 장시간 분석 테스트
- [ ] 이동식 드라이브 분석 중 제거 테스트
- [ ] 네트워크 드라이브 연결 해제 테스트
- [ ] 접근 제한 폴더가 많은 시스템 드라이브 테스트
- [ ] 파일이 지속적으로 생성되는 폴더의 재분석 부하 확인
- [ ] 고해상도 및 다중 모니터 DPI 확인
- [ ] 이미지 대용량 파일과 손상 파일 테스트
- [ ] 다양한 동영상 코덱과 손상 미디어 테스트
- [ ] LGPL 고지, 원문, 버전, 소스 링크 최종 확인
- [ ] 배포 약관에 LGPL 구성요소 역공학 금지 예외 반영
- [ ] 코드 서명 및 설치 프로그램 검토

---

## 18. 현재 결론

ND Explorer는 초기 디자인만 존재하던 상태를 넘어 다음 세 가지 축이 실제 코드로 연결된 상태다.

1. 안전한 단일 패널 파일 탐색 및 파일 작업
2. 내장 이미지·동영상 확인
3. 드라이브별 저장 공간 시각화와 변경 대응

현재 Release 빌드는 성공하며 핵심 미디어와 스캔 서비스 검증도 통과했다. 다음 개발은 저장 공간 분석의 실제 대용량 드라이브 성능을 측정한 뒤, 일반 탐색기의 다중 탭과 듀얼 패널로 확장하는 순서가 적절하다.
